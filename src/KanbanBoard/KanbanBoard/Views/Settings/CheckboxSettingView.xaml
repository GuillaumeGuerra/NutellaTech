<UserControl x:Name="userControl" x:Class="KanbanBoard.Views.CheckboxSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="500" d:DesignHeight="55">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding Header, ElementName=userControl}" Grid.Column="0"/>
        <CheckBox IsChecked="{Binding CheckboxValue, ElementName=userControl}" Grid.Column="1"/>
    </Grid>
</UserControl>
