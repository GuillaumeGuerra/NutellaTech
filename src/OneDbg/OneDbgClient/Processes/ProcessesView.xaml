<UserControl x:Class="OneDbgClient.Processes.ProcessesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" DataContext="{Binding Processes, Mode=OneWay, Source={StaticResource Locator}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Content="{Binding Header}" Grid.Row="0" HorizontalAlignment="Left"/>
        <Button Content="Refresh Processes" Command="{Binding RefreshProcessesCommand}" Grid.Row="0" HorizontalAlignment="Right"/>
        <DataGrid ItemsSource="{Binding AllProcesses}" Grid.Row="1">

        </DataGrid>
    </Grid>
</UserControl>
